{% extends 'base.html.twig' %}

{% block title %}categorie
{% endblock %}

{% block body %}
<div class="container" id="fond"></div>
	<div class="container">

		<div class="row">
			<h1>
				catégories</h1>
			{% for cat in cats %}

				<div class="col-4">
					<div class="card border-dark mb-3 mt-4" style="max-width: 20rem;">
						<div class="card-header">{{cat.libelle}}</div>
						<div class="card-body">
							<img src="images_the_district/category/{{cat.image}}" width="200">
							<h4 class="card-title">
								<a class="btn btn-dark mt-4" href="{{path('app_catplat',{categorie:cat.id})}}" >liste des plats</a>
							
							</div>
						</div>
					</div>
				{% endfor %}
				<div class="navigation d-flex justify-content-center mt-4">
					{{ knp_pagination_render(cats) }}
				</div>
			{% endblock %}
