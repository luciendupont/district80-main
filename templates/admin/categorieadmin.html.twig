{% extends "base.html.twig" %}

{% block body %}

<div class="card border-dark mb-3" style="max-width: 20rem;">
  <div class="card-header">espace admin </div>
  <div class="card-body">
    <p class="card-text">    <td><a href="{{path('app_nouvellecategorie')}}" class="btn btn-dark"> créer une catégorie</a></td>
    </p>
  </div>
</div>


<table class="table table-secondary">
  <thead>
    <tr>
      <th scope="col">id</th>
      <th scope="col">libelle</th>
      <th scope="col">image</th>
      <th scope="col">modifier</th>
      <th scope="col">supprimer</th>
    
    </tr>
  </thead>
  <tbody>
    {% for cat in cats %}
        
   
<tr class="table-active">
    <th scope="row">{{cat.id}}</th>
    <td>{{cat.libelle}}</td>
    <td>{{cat.image}}</td>
    <td><a href="" class="btn btn-dark">modifier</a></td>
    <td><a href="" class="btn btn-light">supprimer</a></td>

  </tr>
   {% endfor %}
</tbody>
</table>


{% endblock %}